class DirectionModel {
  int durationValue, distanceValue;
  String durationText, distanceText, encodedPoints;
  DirectionModel(
      {this.distanceValue,
      this.durationValue,
      this.durationText,
      this.distanceText,
      this.encodedPoints});
}
